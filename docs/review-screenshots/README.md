# Preview review screenshots

These curated screenshots were generated from the local preview candidate with placeholder content. They are not evidence of a live Vercel deployment and must not be treated as final approved marketing assets.

Regenerate them with `npm run review:screenshots` only after reviewing intentional content or layout changes. The full visual-regression baselines remain under `tests/e2e/`.
